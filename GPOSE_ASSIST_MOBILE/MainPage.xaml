<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GPOSE_ASSIST_MOBILE.MainPage"
			 >

	<ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25"
			Loaded="Layout_Loaded"
			Unloaded="Layout_Unloaded">
           

            <Label
                Text="Hello, World!"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label
                Text="Welcome to &#10;.NET Multi-platform App UI"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I" />

            <Button
                x:Name="CounterBtn"
                Text="Click me" 
                SemanticProperties.Hint="Counts the number of times you click"
                Clicked="OnCounterClicked"
                HorizontalOptions="Fill" />
			<VerticalStackLayout>
				<HorizontalStackLayout Margin="1" HorizontalOptions="Center">
					<Button x:Name="BTN_KEY_W" Text="" HorizontalOptions="Center" SemanticProperties.Hint="Send Key W" ImageSource="key_w.png" BackgroundColor="#00000000" BorderColor="#00000000" SemanticProperties.Description="EVENT SEND_KEY_W" Clicked="BTN_Clicked">
					</Button>
				</HorizontalStackLayout>
				<HorizontalStackLayout Margin="1" HorizontalOptions="Center">
					<Button x:Name="BTN_KEY_A" Text="" HorizontalOptions="Center" SemanticProperties.Hint="Send Key A" ImageSource="key_a.png" SemanticProperties.Description="EVENT SEND_KEY_A" Clicked="BTN_Clicked">
					</Button>
					<Button x:Name="BTN_KEY_D" Text="" HorizontalOptions="Center" SemanticProperties.Hint="Send Key D" ImageSource="key_d.png" SemanticProperties.Description="EVENT SEND_KEY_D" Clicked="BTN_Clicked" >
					</Button>
				</HorizontalStackLayout>
				<HorizontalStackLayout Margin="1" HorizontalOptions="Center">
					<Button x:Name="BTN_KEY_S" Text="" HorizontalOptions="Center" SemanticProperties.Hint="Send Key S" ImageSource="key_s.png" SemanticProperties.Description="EVENT SEND_KEY_S" Clicked="BTN_Clicked">
					</Button>
				</HorizontalStackLayout>
			</VerticalStackLayout>
			
		</VerticalStackLayout>
    </ScrollView>

</ContentPage>
